<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyc.test.mapper.StockMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lyc.test.model.Stock">
        <id column="id" property="id" />
        <result column="goods_id" property="goods_id" />
        <result column="num" property="num" />
        <result column="price" property="price" />
        <result column="total_money" property="total_money" />
        <result column="order_no" property="order_no" />
        <result column="create_time" property="create_time" />
        <result column="stock_type" property="stock_type" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, goods_id, num, price, total_money, order_no, create_time, stock_type
    </sql>

</mapper>
